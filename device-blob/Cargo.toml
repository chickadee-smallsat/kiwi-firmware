[package]
name = "device-blob"
version = "0.0.1"
edition = "2024"

[dependencies]
heapless = { version = "0.9" }
defmt = { version = "1.0", optional = true }
crc16 = "0.4"
aligned = "0.4"

[features]
defmt = ["dep:defmt", "heapless/defmt"]
